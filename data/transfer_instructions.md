On this machine, run:

python edge/esp32/tools/export_sessions.py E:/pdm_transfer --skip-tests

once done, move USB to other machine and copy everything off the USB into that machine's data/raw/ folder