# Template for .env — copy to .env and fill in real values.
# .env is gitignored; this template is tracked.
#
# InfluxDB 3 Core admin token (database master credential).
# Generate with:  influxdb3 create token --admin
INFLUXDB_TOKEN=
INFLUXDB_URL=http://localhost:8181
INFLUXDB_DATABASE=e2c_pdm
